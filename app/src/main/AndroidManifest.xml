<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RIGSWAP">

        <!-- Login / Entry Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Bottom Navigation Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <!-- Product Details -->
        <activity
            android:name=".ui.detail.ProductDetailActivity"
            android:exported="false" />

        <!-- Buyer-Seller Chat -->
        <activity
            android:name=".ui.chat.ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Saved Rig Specs -->
        <activity
            android:name=".ui.profile.SavedRigSpecsActivity"
            android:exported="false" />

        <!-- Compatible Parts View -->
        <activity
            android:name=".ui.profile.CompatiblePartsActivity"
            android:exported="false" />

        <!-- Merchant Hub / Seller Dashboard -->
        <activity
            android:name=".ui.profile.MerchantHubActivity"
            android:exported="false" />

        <!-- My Active Listings -->
        <activity
            android:name=".ui.profile.MyListingsActivity"
            android:exported="false" />

        <!-- Shopping Cart & Checkout -->
        <activity
            android:name=".ui.profile.CartActivity"
            android:exported="false" />
        <!-- FileProvider for high-resolution camera photo capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>